// Components
import { AdminContainer } from '@components/admin';
import { PageTransition } from '@components/shared/animations';

export default function Order() {
    return (
        <AdminContainer>
            <PageTransition>Order</PageTransition>
        </AdminContainer>
    );
}
